.row.weather-data
	.col-xs-1.first-block
		%p
	.col-xs-1
		%p=dateFormat(temps.created_at, 0).strftime("%A")
		%p=dateFormat(temps.created_at, 0).strftime("%m-%d-%Y")
	.col-xs-1
		%p=dateFormat(temps.created_at, 1).strftime("%A")
		%p=dateFormat(temps.created_at, 1).strftime("%m-%d-%Y")
	.col-xs-1
		%p=dateFormat(temps.created_at, 2).strftime("%A")
		%p=dateFormat(temps.created_at, 2).strftime("%m-%d-%Y")
	.col-xs-1
		%p=dateFormat(temps.created_at, 3).strftime("%A")
		%p=dateFormat(temps.created_at, 3).strftime("%m-%d-%Y")
	.col-xs-1
		%p=dateFormat(temps.created_at, 4).strftime("%A")
		%p=dateFormat(temps.created_at, 4).strftime("%m-%d-%Y")
	.col-xs-1
		%p=dateFormat(temps.created_at, 5).strftime("%A")
		%p=dateFormat(temps.created_at, 5).strftime("%m-%d-%Y")
	.col-xs-1
		%p=dateFormat(temps.created_at, 6).strftime("%A")
		%p=dateFormat(temps.created_at, 6).strftime("%m-%d-%Y")
	.col-xs-1
		%p Average High
	.col-xs-1
		%p Average Low
	.col-xs-1
		%p High
	.col-xs-1
		%p Low

.row
	.col-xs-1.first-block
		%p High (F)
		%p Low (F)
	.col-xs-1
		%p=forecast[1][0]
		%p=forecast[2][0]
	.col-xs-1
		%p=forecast[1][1]
		%p=forecast[2][1]
	.col-xs-1
		%p=forecast[1][2]
		%p=forecast[2][2]
	.col-xs-1
		%p=forecast[1][3]
		%p=forecast[2][3]
	.col-xs-1
		%p=forecast[1][4]
		%p=forecast[2][4]
	.col-xs-1
		%p=forecast[1][5]
		%p=forecast[2][5]
	.col-xs-1
		%p=forecast[1][6]
		%p=forecast[2][6]
	.col-xs-1
		%p=forecast[4]
	.col-xs-1
		%p=forecast[3]
	.col-xs-1
		%p=forecast[1].max
	.col-xs-1
		%p=forecast[2].min